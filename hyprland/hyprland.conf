# Hyprland conf 
# Maintainer : naksudev

##############
## START-UP ##
##############
exec = python3 ~/.config/hypr/scripts/greeting_notif.py
exec-once = swww init & sleep 1; python3 ~/.config/hypr/scripts/wallpaper.py
exec-once = ~/.config/hypr/scripts/sleep.sh
exec-once = waybar
exec-once = dunst
exec-once = udiskie &
exec-once = swayosd-server
exec-once = [workspace special:magic silent;noanim] kitty

###############
## VARIABLES ##
###############
$terminal = kitty
$fileManager = dolphin
$menu = rofi -show drun -theme ~/.config/rofi/launchers/style-5.rasi 

##########
## ENVS ##
##########
env = XCURSOR_SIZE,12

##############
## MONITORS ##
##############

# Primary monitor
monitor=eDP-1, 1920x1080, 0x0, 1

# Secondary monitor
monitor = HDMI-A-1, highres, 1920x0, 1, mirror, eDP-1
workspace = name:S, monitor:HDMI-A-1, default:true

############
## INPUTS ##
############
input {
    kb_layout = fr,us
    kb_options = grp:win_space_toggle
    follow_mouse = 1

    touchpad {
        natural_scroll = true
    }

    sensitivity = 0 
}

##############
## SECTIONS ##
##############
general {
    gaps_in = 5
    gaps_out = 15
    border_size = 2
    col.active_border = rgb(968e7e)
    col.inactive_border = rgba(595959aa)

    layout = dwindle

    allow_tearing = false
}

decoration {
    rounding = 8
    
    blur {
        enabled = false
        size = 0
        passes = 1
    }

    drop_shadow = no
}

animations {
    enabled = true

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    pseudotile = yes 
    preserve_split = yes 
}

master {
    new_is_master = true
}

gestures {
    workspace_swipe = on
}

misc {
    force_default_wallpaper = 0 
    disable_hyprland_logo = true
    background_color = 0x000000
    vfr = true
}

#################
## WINDOW RULE ##
#################
# General
windowrulev2 = nomaximizerequest, class:.* 
windowrulev2 = float, class:(imv), title:(imv)

# Rofi
windowrulev2 = stayfocused, title:(^rofi)
windowrulev2 = pin, title:(^rofi)
windowrulev2 = dimaround, title:(^rofi)

# VirtualBox & VMWare idleinhibiter
windowrulev2 = idleinhibit always, title:(- Oracle VM VirtualBox$)
windowrulev2 = idleinhibit always, title:(- VMWare Workstation$)

# PokeMMO
windowrulev2 = maximize, title:(PokeMMO)

#################
## KEYBINDINGS ##
#################
$mainMod = SUPER

bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, Q, killactive 
bind = $mainMod, M, exec, pkill -f "/home/naksu/.config/hypr/scripts/wallpaper.py" & hyprctl dispatcher exit
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating, 
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Resize active window
bind = $mainMod ALT, left, resizeactive, -10 0
bind = $mainMod ALT, right, resizeactive, 10 0
bind = $mainMod ALT, up, resizeactive, 0 10
bind = $mainMod ALT, down, resizeactive, 0 -10

# Move active window
bind = $mainMod SHIFT, left, movewindow, l
bind = $mainMod SHIFT, right, movewindow, r
bind = $mainMod SHIFT, up, movewindow, u
bind = $mainMod SHIFT, down, movewindow, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 0x26, workspace, 1
bind = $mainMod, 0xE9, workspace, 2
bind = $mainMod, 0x22, workspace, 3
bind = $mainMod, 0x27, workspace, 4
bind = $mainMod, 0x28, workspace, 5
bind = $mainMod, 0x2D, workspace, 6
bind = $mainMod, 0xE8, workspace, 7
bind = $mainMod, 0x5F, workspace, 8
bind = $mainMod, 0xE7, workspace, 9
bind = $mainMod, 0xE0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 0x26, movetoworkspace, 1
bind = $mainMod SHIFT, 0xE9, movetoworkspace, 2
bind = $mainMod SHIFT, 0x22, movetoworkspace, 3
bind = $mainMod SHIFT, 0x27, movetoworkspace, 4
bind = $mainMod SHIFT, 0x28, movetoworkspace, 5
bind = $mainMod SHIFT, 0x2D, movetoworkspace, 6
bind = $mainMod SHIFT, 0xE8, movetoworkspace, 7
bind = $mainMod SHIFT, 0x5F, movetoworkspace, 8
bind = $mainMod SHIFT, 0xE7, movetoworkspace, 9
bind = $mainMod SHIFT, 0xE0, movetoworkspace, 10

bind = $alttKey, TAB, exec, rofi -show window

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1
,
# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Brightness control
bind = , XF86MonBrightnessUp, exec, brightnessctl s +4800 && swayosd-client --brightness raise
bind = , XF86MonBrightnessDown, exec, brightnessctl s 4800- && swayosd-client --brightness lower

# Screenshot
bind = , PRINT, exec, hyprshot -c --clipboard-only -m output
bind = $mainMod, PRINT, exec, hyprshot --clipboard-only -c -m window
bind = $shiftMod, PRINT, exec, hyprshot --clipboard-only -m region

# Audio control
bind = , XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5% && swayosd-client --output-volume raise --max-volume 100
bind = , XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5% && swayosd-client --output-volume lower --max-volume 100 
bind = , XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioNext, exec, playerctl next
bind = , XF86AudioPrev, exec, playerctl previous
bind = , XF86audiostop, exec, playerctl stop

# Lock screen
bind = $mainMod, L, exec, swaylock -f --config ~/.config/swaylock/config

# Color Picker
bind = $mainMod, I, exec, hyprpicker -a

# Power button
bind = , XF86PowerOff, exec, systemctl suspend & swaylock
